# Deployment

Prompt 10 permits a private GitHub repository and safe hosted environment only
after exact owner and plan preflights.

Required order:

1. reproduce the local lockfile install, eight migrations and complete QA;
2. push the immutable reviewed candidate to
   `Metric-One/meta-automation-crm`;
3. run CI with isolated local Supabase;
4. verify a commercially eligible host;
5. create/link one exact project;
6. configure Preview/Staging secrets separately and directly;
7. deploy invite-only, synthetic-only, Meta Sandbox and
   `LIVE_PROVIDER_SEND_ENABLED=false`;
8. link an approved Supabase Staging project and Inngest non-production
   environment;
9. run hosted security/recovery/load evidence before any Production request.

The currently verified Vercel team is Hobby and therefore blocked for this
commercial service. No Vercel project or deployment may be created on that
assumption. Prompt 11 handles owner values and commercially eligible hosting.
