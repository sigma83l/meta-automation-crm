# Prompt 10 Accessibility Evidence

- semantic landmarks, labels, descriptions and status announcements;
- visible `:focus-visible` treatment and 44px touch targets;
- logical CSS properties and document-level `dir=rtl` for Persian;
- reduced-motion media query and no required animation;
- Light/Dark semantic foreground, border and surface pairs;
- keyboard order follows DOM order in mirrored layouts;
- Playwright route, mobile overflow and RTL smoke passed.

Formal third-party WCAG certification remains outside this engineering smoke.
