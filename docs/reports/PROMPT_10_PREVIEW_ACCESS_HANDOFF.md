# Prompt 10 Preview Access Handoff

The final private URL and allowed owner identity are recorded only after the
hosted gate passes. MANI will open `/signup`, use the allowlisted owner email,
choose a private password in the browser, and enter the automatically created
workspace. No password, token or secret is stored here.
