# Prompt 10 Preview Access Handoff

Status: **BLOCKED — no Preview URL**

The Vercel project exists, but all three deployment attempts were unexpectedly
classified as Production and were removed. MANI must not use a Production URL
as Preview evidence.

After the host-target issue is resolved, MANI will open `/signup` on the private
Preview, use the allowlisted owner email, choose a private password in the
browser, and enter the automatically created workspace. No password, token or
secret is stored here.
