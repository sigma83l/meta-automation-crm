# Fresh migrations only

Prompt 0 applies no database migration. Prompt 1 will add new Supabase migrations
for authentication, workspace ownership, row-level security, and the private CRM
Storage bucket. Donor migration history must never be copied here.
